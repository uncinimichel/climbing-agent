anthropic>=0.60
rich>=13
fastapi>=0.115
uvicorn>=0.30
jsonschema>=4.20

# ingest/ theCrag/UKC browser scrapers (roadmap) — this venv is shared, run
# `playwright install chromium` once after installing.
playwright>=1.61
beautifulsoup4>=4.15
