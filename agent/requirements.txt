anthropic>=0.60
psycopg[binary]>=3.2
rich>=13
fastapi>=0.115
uvicorn>=0.30

# db/tools/crawl_worker.py + theCrag/UKC scrapers (roadmap Stage 5) — this venv
# is shared, run `playwright install chromium` once after installing.
playwright>=1.61
beautifulsoup4>=4.15
