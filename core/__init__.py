"""Sport-agnostic tools: weather providers + derived metrics, travel (flights,
stays), caching, HTTP, geo, quota, scoring primitives, the trip context and the
Sport port every domain implements.

Nothing in here knows what a crag, a piste or a fairway is. If a change here
needs the word "climbing", it belongs in domains/climbing instead.
"""
