# Curation Studio — runs on the JSON record (decision #39), no database.
fastapi==0.139.0
uvicorn==0.50.0
pydantic==2.13.4
python-multipart==0.0.32
pillow==12.3.0
jsonschema==4.25.1
# Legacy Postgres escape hatch (db/sql/ + ingest_corpus.py) additionally needs:
#   psycopg[binary]==3.3.4
